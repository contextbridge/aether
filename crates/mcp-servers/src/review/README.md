# Review MCP

Review MCP exposes `review_artifact`, which reads a Markdown file from the agent-side workspace and opens the existing Markdown review interface through MCP elicitation. It never creates or modifies artifacts.
