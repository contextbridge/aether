# Review MCP

This MCP exposes a `review_artifact` tool that the agent can use to request annotated feedback on a Markdown file or string, or on an HTML artifact reviewed in the browser with element-level comments.
